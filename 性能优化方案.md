# 运行调度性能优化

## 1. 背景与目标

Agent Bench 的运行调度详情页主要面向测试工程师。用户通常需要在一个包含数十到数百条用例的任务中，频繁执行以下操作：

- 打开任务详情并查看用例执行情况。
- 在不同分页之间切换。
- 点击 Pass、Failed、Error 快速定位对应结果。
- 打开某条用例详情排查执行结果。

本次优化的目标是在不改变运行状态、统计口径、接口业务语义和历史数据的前提下，减少磁盘读取、全量数据复制、重复计算及重复网络请求，使上述高频操作保持稳定、快速。

## 2. 问题

### 2.1 打开任务详情请求和数据量偏大

优化前，任务详情页需要分别请求任务信息和用例列表。任务信息中还包含完整的工作流结构快照，而详情列表实际只需要任务名称、状态、测试集、工作流、展示字段和起止时间等轻量信息。

主要问题：

- 页面首次进入需要发起两次请求。
- 完整工作流结构快照增加了序列化、传输和前端解析成本。
- 两个请求任意一个较慢，都会推迟详情页完整展示。

### 2.2 分页查询重复处理全部用例

优化前，每次分页查询都会读取当前任务下的全部用例 JSON，随后进行排序、筛选和深拷贝，最后才截取当前页。

当任务包含大量用例时，即使用户只查看 10 条数据，也需要为全部用例支付磁盘读取、排序和对象复制成本。分页次数越多，重复工作越明显。

### 2.3 Pass、Failed、Error 筛选重复全量扫描

每次点击结果筛选都需要重新遍历全部用例，并根据执行状态和评估结果计算 Pass、Failed、Error。统计数量和筛选列表也会重复计算。

主要问题：

- 每次筛选都执行相同的结果归类逻辑。
- 分页切换后再次重复扫描。
- 搜索时需要反复拼接和标准化用例文本。

### 2.4 打开用例详情存在重复请求

当前分页接口已经返回了展示该页所需的完整用例数据，但点击“查看用例详情”时，前端仍会再次请求同一条用例，然后再请求对应的工作流执行详情；Error 用例还会额外请求节点执行记录。

这次重复的用例请求不会提供新信息，却会增加用例详情的等待时间。

## 3. 解决思路

本次方案遵循四个原则：

1. 减少请求次数：任务详情使用一个聚合接口完成首屏加载，用例详情复用当前页已有数据。
2. 缩小响应体积：详情列表只返回轻量任务信息，不返回工作流结构快照。
3. 避免重复计算：为当前任务用例建立内存索引，缓存排序、结果分类、统计和搜索文本。
4. 保证数据正确：任何用例写入、任务重建或删除都会使对应查询索引失效，下一次查询重新构建。

缓存只负责加速读取，不作为新的事实来源。磁盘中的运行数据仍然是最终数据来源。

## 4. 实施方案

### 4.1 用例数据缓存

`BatchExecutionStore` 增加两级进程内缓存：

- `_case_cache`：以用例目录为键，保存 `case_id -> case` 的映射，避免每次分页重新读取全部 JSON 文件。
- `_case_query_cache`：保存基于当前用例数据构建的查询索引。

查询索引包含：

- `ordered_ids`：按调用顺序排好的用例 ID。
- `result_ids`：Pass、Failed、Error 对应的用例 ID 集合。
- `summary`：三类结果的数量统计。
- `search_texts`：预先标准化的小写搜索文本。

首次查询仍需读取并索引全部用例，后续分页和结果筛选直接复用索引。

### 4.2 服务端分页

统一通过 `query_cases(...)` 完成用例查询：

1. 从缓存获取用例映射和查询索引。
2. 根据 `result` 直接选择对应结果 ID 集合。
3. 如有搜索词，在缓存的标准化文本中匹配。
4. 根据 `page` 和 `page_size` 截取当前页 ID。
5. 仅对当前页用例执行深拷贝并返回。

复杂度变化如下：

| 场景 | 优化前 | 优化后 |
| --- | --- | --- |
| 首次查询 | 读取、排序全部用例 | 读取、排序并建立一次索引 |
| 后续普通分页 | 全量读取、排序、复制后分页 | 按索引截取，仅复制当前页 |
| Pass/Failed/Error 筛选 | 每次全量计算结果 | 直接复用结果 ID 集合 |
| 文本搜索 | 全量读取并重新拼接文本 | 扫描缓存文本，不再读取文件或重复拼接 |

其中 N 为任务总用例数，P 为每页用例数。建立索引后的普通分页和结果筛选主要成本由 O(N) 降为 O(P)；文本搜索仍需扫描候选文本，但避免了磁盘读取、对象深拷贝和文本重复构造。

### 4.3 任务详情聚合接口

`GET /api/batch-runs/{batch_id}/cases` 同时返回：

- `batch`：详情页所需的轻量任务信息。
- `cases`：当前页用例。
- `total`：当前筛选条件下的用例总数。
- `total_all`：任务全部用例数。
- `summary`：Pass、Failed、Error 数量。
- `page`、`page_size`：分页信息。

接口支持：

- `result=Pass|Failed|Error`
- `q=搜索词`
- `round_id=历史执行轮次`
- `page` 和 `page_size`

前端打开任务详情时只发起这一个请求，不再并行请求完整任务对象。轻量 `batch.workflow` 仅保留工作流 ID 和名称，不包含 `structural_snapshot`。

### 4.4 用例详情复用当前页数据

任务详情渲染当前页时，前端建立 `caseRunsById` 映射。用户点击“查看用例详情”后：

1. 优先从 `caseRunsById` 获取当前用例。
2. 直接请求对应的工作流执行详情。
3. 只有 Error 用例需要进一步请求节点执行记录。
4. 如果当前页快照不存在，才回退到单条用例接口，保证其他调用场景兼容。

请求数量变化：

| 操作 | 优化前 | 优化后 |
| --- | --- | --- |
| 打开任务详情 | 完整任务请求 + 用例分页请求 | 1 次聚合分页请求 |
| 打开 Pass/Failed 用例详情 | 用例请求 + 工作流执行请求 | 仅工作流执行请求 |
| 打开 Error 用例详情 | 用例请求 + 工作流执行请求 + 节点记录请求 | 工作流执行请求 + 节点记录请求 |

### 4.5 缓存一致性

以下操作会更新用例缓存并清除查询索引：

- 写入单条用例执行结果。
- 创建任务用例快照。
- 恢复或替换当前执行轮次。
- 删除任务。

索引失效后，下一次查询会基于最新用例数据重新构建统计、结果分组和搜索文本，避免页面继续展示旧结果。

缓存由写锁保护，防止调度线程写入用例的同时，详情查询读取到不完整状态。

## 5. 验收标准与验证结果

### 5.1 功能验收

- 任务详情只发起一次用例聚合请求。
- 分页只返回当前页用例，数量和页码正确。
- Pass、Failed、Error 的数量和筛选结果与真实执行结果一致。
- 用例执行结果更新后，筛选统计能够立即刷新。
- 打开当前页用例详情时不重复请求单条用例。
- 历史执行轮次、文本搜索和手动执行用例行为保持不变。

### 5.2 自动化验证

- 用例 Store、API、调度器和前端相关测试通过。
- 全量测试结果：328 passed，4 skipped。
- `npm run build` 构建通过。
- `node --check web/static/execution.js` 语法检查通过。
- `git diff --check` 通过。

### 5.3 端到端验证

使用本地包含 100 条用例的真实任务完成浏览器回归：

- 任务详情正常打开并展示 10 条当前页数据。
- Pass、Failed、Error 筛选结果和数量正确。
- 筛选结果可以正常分页。
- 用例详情可以正常打开，错误用例仍能展示节点和调试信息。
- 本地验证中，上述详情打开和筛选交互均在 1 秒内完成。

## 6. 边界与后续方向

- 当前缓存为单进程内存缓存，应用重启后会按需重建；这符合本机单用户部署场景。
- 缓存内存占用与任务用例数量近似线性增长。当前数十到数百条用例的典型规模可接受；若未来出现数万条用例，需要增加缓存淘汰策略或迁移到数据库索引分页。
- 文本搜索仍需遍历候选用例的缓存文本。若搜索成为新的主要瓶颈，可进一步增加分词索引或 SQLite FTS，但当前没有必要引入额外复杂度。
- 运行中的任务会持续写入用例结果，因此查询索引会按写入失效并重建。该策略优先保证数据实时正确；如未来并发量显著增加，可改为增量更新单条结果索引。

## 7. 工作流执行存储优化

### 7.1 业务背景与目标

运行调度的核心用户是需要批量验证 Agent 并定位失败原因的测试工程师。用户在任务运行期间关注进度和通过率，在任务结束后需要进入单条用例查看完整 Context、原始错误和节点日志。

当前 100 条用例经过多轮执行后，运行目录在一天内即可超过 1 GiB。存储增长会占满本机磁盘，并拖慢目录扫描、备份和历史查询。优化目标不是减少排障证据，而是在保持详情透明可读的前提下，消除重复载荷并缩短热数据驻留时间。

第一阶段遵循以下边界：

1. 不截断 Failed、Error、Interrupt 或 Pass 的原始日志。
2. 不删除现有运行数据，不改变任务和用例结果语义。
3. 归档后的工作流详情和节点日志继续通过原接口读取。
4. 只对终态执行归档，PENDING 和 RUNNING 不参与。
5. 数据生命周期删除和任务级联删除必须单独确认后实施。

### 7.2 实测基线

2026-08-01 本机样本统计：

| 指标 | 实测值 |
| --- | ---: |
| `run_storage` 总占用 | 约 1.27 GiB，运行中仍在增长 |
| `workflow_executions` 占比 | 约 99.6% |
| `batch_executions` | 约 1.8 MiB |
| SQLite | 约 3.9 MiB |
| 热工作流执行数量 | 461 |
| 完整执行中位大小 | 约 4.18 MiB |
| 无批量用例引用的 BATCH 执行 | 178 个，约 186 MiB |

单个 4.276 MiB 完整执行的无损实验结果：

| 方案 | 结果 | 节省 |
| --- | ---: | ---: |
| 紧凑 JSON | 2.610 MiB | 39.0% |
| 现有 ZIP + 完整 Workflow Manifest | 1.312 MiB | 69.3% |
| 单看 ZIP 内容 | 0.273 MiB | 93.6% |
| 紧凑 JSON + 去重引用模拟 | 0.592 MiB | 86.2% |

归档读取基准：

| 读取内容 | 热目录中位数 | 冷归档中位数 |
| --- | ---: | ---: |
| 工作流详情 | 5.0 ms | 10.5 ms |
| 全部节点日志 | 19.7 ms | 24.3 ms |

当前瓶颈来自同一载荷被多次物化：

- Workflow 同时保存 `context.commits[].values` 和完整 `context.final`。
- SCRIPT 和 END 节点保存执行时的完整 Context 到 `inputs`。
- HTTP 响应同时存在于 `attempts.response`、顶层 `response` 和 `outputs`。
- Workflow Execution JSON 使用两空格缩进保存。
- 冷归档默认等待 7 天，同一天产生的数据全部驻留热目录。
- 归档 Manifest 再次保存完整 Workflow，抵消部分 ZIP 收益。
- 删除任务不会回收关联的 Workflow Execution，形成孤立数据。

### 7.3 业界参考

- [MLflow Trace Archival](https://mlflow.org/docs/latest/self-hosting/architecture/tracking-server/#trace-archival)：只归档终态 Trace，将载荷移入 Artifact Store，同时保持 UI 和 API 可读，并限制单轮归档数量。
- [Grafana Tempo](https://grafana.com/docs/tempo/latest/configuration/)：近期 Trace 使用 Live Store/WAL，历史 Trace 进入对象存储，通过 Compaction、Retention 和载荷上限控制容量。
- [Temporal Cloud Limits](https://docs.temporal.io/cloud/limits)：限制单请求和 Workflow History 体积，并建议大型 Payload 存入外部存储，执行历史只保留引用。

本项目是本机单用户工具，不需要直接引入分布式对象存储，但应采用相同原则：轻量元数据用于列表，完整载荷按需读取，终态数据快速压缩，大对象只保存一份。

### 7.4 分阶段实施

#### 第一阶段：无损热冷分层

- 将 Manifest 升级为轻量版本，只保存执行 ID、状态和时间等列表索引字段。
- 兼容读取已有包含完整 Workflow 的旧版 Manifest。
- 建立 `execution_id -> archive` 进程内索引，详情读取不再逐个扫描 Manifest。
- 终态执行保留在热目录 15 分钟，之后自动归档。
- 归档调度每 5 分钟执行一次，每轮最多处理 100 个执行，避免启动时长时间阻塞。
- 新写入 Workflow Execution JSON 使用无损紧凑格式。
- 最近运行、任务详情和节点日志接口保持不变。

#### 第二阶段：单一载荷与引用

- 将大 Context、HTTP/LLM 原始响应保存为不可变压缩 Blob。
- Workflow、Node 和 Attempt 仅保存内容摘要、字段索引和 Blob 引用。
- SCRIPT 节点保存 Context 版本引用，不复制完整 Context。
- 使用内容哈希和引用计数保证去重数据不会被提前回收。
- 为冷数据增加小容量 LRU 解压缓存。

#### 第三阶段：生命周期治理

- 增加孤立终态执行扫描和宽限期回收。
- 删除任务时，在用户确认后级联清理其 Workflow Execution。
- 为成功、失败和中断结果配置不同保留期。
- 在设置页展示当前热数据、冷数据、孤立数据和预计可回收空间。

### 7.5 验收标准

- 归档前后完整 Workflow、Context、节点日志和错误对象深度相等。
- 100 条完整用例结束后 5 分钟内，冷存储目标不超过 40 MiB。
- 第二阶段完成后，执行期间峰值增量不超过 100 MiB / 100 条。
- 冷数据详情读取 P95 小于 2 秒，本机目标小于 100 ms。
- 列表查询不读取或解压完整节点日志。
- 应用在 ZIP 或 Manifest 发布中断后能够恢复，不丢失可验证的热数据。
- 归档任务有单轮上限，不阻塞应用启动和运行调度。

### 7.6 实施状态

- [x] 精简 Manifest 并兼容旧格式。
- [x] 增加归档位置索引。
- [x] 将热保留改为终态后 15 分钟。
- [x] 增加每轮最多 100 个执行的归档上限。
- [x] Workflow Execution JSON 改为紧凑写入。
- [x] 完成透明详情读取、恢复和容量回归测试。
- [ ] 设计大载荷 Blob 与引用模型。
- [ ] 确认生命周期删除策略后实现孤立数据回收。

### 7.7 第一阶段实施结果

2026-08-01 已完成无损热冷分层。对一个真实的 4.276 MiB Workflow Execution 样本进行深度相等校验，归档前后的 Workflow、Context、节点日志和错误对象一致，结果如下：

| 指标 | 实测值 |
| --- | ---: |
| 原始执行目录 | 4.276 MiB |
| 紧凑热数据 | 2.610 MiB |
| 新版冷归档总量 | 0.253 MiB |
| 冷归档容量下降 | 94.1% |
| 轻量 Manifest | 约 0.46 KiB |
| 热节点日志读取中位数 | 16.22 ms |
| 冷节点日志读取中位数 | 14.25 ms |
| 冷历史列表读取中位数 | 3.74 ms |

验证结果：

- 工作流 Store、API、批量调度、前端等全量自动化测试：330 passed，4 skipped。
- 受影响模块回归：91 passed。
- `npm run build`、Python 编译和 `node --check web/static/execution.js` 均通过。
- 归档后的详情和节点日志继续通过原接口透明读取，前端无需区分热数据和冷数据。
- 本轮未迁移、删除或归档现有 `run_storage` 数据；应用重启后，新调度器才会按每轮最多 100 个执行处理超过 15 分钟的终态数据。

### 7.8 100 条用例真实运行基准

2026-08-01 使用任务“机房故障诊断批量验证-20260731-214742”完成 100 条用例、10 并发的端到端容量回归。测试前删除该任务 10 个历史轮次和 621 条关联 Workflow Execution，共回收 2556.24 MiB；任务配置、测试集和 Workflow 保持不变。

有效基线和结果如下：

| 阶段 | 总存储 | 目标任务热执行 | 目标任务冷归档 | 相对基线净增长 |
| --- | ---: | ---: | ---: | ---: |
| 清理并启动新版服务后 | 16.55 MiB | 0 MiB | 0 MiB | 0 MiB |
| 100 条执行完成 | 258.12 MiB | 241.49 MiB | 0 MiB | 241.57 MiB |
| 自动归档完成 | 39.04 MiB | 0 MiB | 22.33 MiB | 22.49 MiB |

运行与归档结果：

- 任务耗时约 6 分 26 秒，最终状态为 SUCCESS，100 Success、100 Pass，无 Failed、Error 或 Interrupt。
- 10 个并发槽位在主要执行阶段保持占满，100 条用例共产生 100 条 Workflow Execution，没有失败重试带来的额外执行。
- 热数据从 241.49 MiB 压缩为 22.33 MiB，下降 90.8%；冷归档平均约 0.223 MiB / 用例。
- 归档器按每条执行的 `finished_at` 判断年龄，100 条执行分为 81、10、9 三批完成归档，未提前归档最后结束的用例。
- 抽查 CASE-001：热、冷执行详情均为 482591 字节，11 条节点日志均为 2051037 字节；两组 SHA-256 哈希分别完全一致。
- 冷详情重复读取 20 次：详情 P95 为 34.71 ms，节点日志 P95 为 58.43 ms；首次冷详情读取为 153.26 ms，均低于 2 秒验收线。
- 归档完成后任务详情、100 条用例列表、执行详情和节点日志接口均返回成功，服务错误日志为 0。
